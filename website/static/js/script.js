function preview(ele){
    var file = ele.files;
    var reader  = new FileReader();

}
(function setup() {
const firstImg = document.querySelector('.image-upload-container .image');
const input = document.querySelector('.image-upload-container input');
const imageTpl = firstImg.outerHTML;
firstImg.addEventListener('click', e => { input.click(); });
input.addEventListener('change', e => {
 
});

})();